---
phase: 01-quick-wins
plan: 01
type: execute
---

<objective>
Appliquer les corrections "Quick Wins" identifiées dans l'audit UI pour améliorer immédiatement l'expérience utilisateur.

Purpose: Corrections à fort impact avec effort minimal, ciblant les incohérences visuelles les plus visibles.
Output: CSS et composants corrigés, score UI amélioré sur les aspects critiques.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UI-AUDIT-REPORT.md

**Key files:**
@frontend/assets/css/design-system.css
@frontend/assets/css/design-tokens.css
@frontend/components/dashboard/IntegrationsStatus.vue
@frontend/pages/dashboard/platforms/index.vue
@frontend/components/products/EditorFooter.vue

**Issues addressed (from UI-AUDIT-REPORT.md):**
- TYP-02: Texte "Bienvenue" trop léger sur le dashboard
- COL-02: Bouton "Annuler" en bg-gray (déjà correct, vérifier uniformité)
- COL-03: Badge "Déconnecté" avec contraste faible (severity="secondary")
- CMP-01: Badges avec styles mixtes (pill vs rectangle)

**Constraints:**
- Ne pas casser les composants PrimeVue existants
- Utiliser les design tokens existants
- Garder la compatibilité avec le thème jaune/or
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status color tokens to design system</name>
  <files>frontend/assets/css/design-tokens.css</files>
  <action>
    Ajouter des variables CSS pour les couleurs de statut sémantiques dans la section "COLORS - Semantic":

    ```css
    /* Status Colors (semantic mapping) */
    --color-status-connected: var(--color-success-500);
    --color-status-connected-bg: var(--color-success-100);
    --color-status-disconnected: var(--color-gray-500);
    --color-status-disconnected-bg: var(--color-gray-100);
    --color-status-published: var(--color-success-500);
    --color-status-published-bg: var(--color-success-100);
    --color-status-draft: var(--color-gray-500);
    --color-status-draft-bg: var(--color-gray-100);
    --color-status-sold: var(--color-info-500);
    --color-status-sold-bg: var(--color-info-100);
    --color-status-inactive: var(--color-warning-500);
    --color-status-inactive-bg: var(--color-warning-100);
    ```

    Ajouter après la section "COLORS - UI".
  </action>
  <verify>grep -q "color-status-connected" frontend/assets/css/design-tokens.css</verify>
  <done>Variables CSS de statut ajoutées dans design-tokens.css</done>
</task>

<task type="auto">
  <name>Task 2: Add badge pill classes and status badge utilities</name>
  <files>frontend/assets/css/design-system.css</files>
  <action>
    Dans la section "3. BADGES & TAGS", modifier le badge de base pour utiliser pill shape et ajouter des classes utilitaires pour les badges de statut:

    1. Modifier `.badge` pour utiliser `rounded-full` au lieu de `rounded-lg`:
    ```css
    .badge {
      @apply inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium;
    }
    ```

    2. Ajouter les classes de badge status après `.badge-primary`:
    ```css
    /* Badge Statut - Connected/Disconnected */
    .badge-status-connected {
      @apply bg-success-100 text-success-700;
    }

    .badge-status-disconnected {
      @apply bg-gray-200 text-gray-600;
    }

    .badge-status-published {
      @apply bg-success-100 text-success-700;
    }

    .badge-status-draft {
      @apply bg-gray-100 text-gray-600;
    }

    .badge-status-sold {
      @apply bg-info-100 text-info-700;
    }

    .badge-status-inactive {
      @apply bg-warning-100 text-warning-700;
    }
    ```
  </action>
  <verify>grep -q "badge-status-connected" frontend/assets/css/design-system.css && grep -q "rounded-full" frontend/assets/css/design-system.css</verify>
  <done>Badge base utilise pill shape (rounded-full), classes status ajoutées</done>
</task>

<task type="auto">
  <name>Task 3: Improve dashboard subtitle contrast</name>
  <files>frontend/assets/css/design-system.css</files>
  <action>
    Dans la section "7. TYPOGRAPHIE", modifier `.page-subtitle` pour améliorer le contraste:

    Changer de `text-gray-600` à `text-gray-700` (plus foncé):
    ```css
    .page-subtitle {
      @apply font-body text-gray-700 mt-1;
    }
    ```

    Cela améliore le contraste du texte "Bienvenue" sur le dashboard (TYP-02).
  </action>
  <verify>grep "page-subtitle" frontend/assets/css/design-system.css | grep -q "text-gray-700"</verify>
  <done>Contraste du subtitle amélioré (gray-600 → gray-700)</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Tous les tokens de statut sont présents dans design-tokens.css
- [ ] Le badge de base utilise `rounded-full` (pill shape)
- [ ] Les classes `.badge-status-*` sont définies
- [ ] Le `.page-subtitle` utilise `text-gray-700`
- [ ] `npm run dev` démarre sans erreur (hot-reload si déjà lancé)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No CSS syntax errors
- Design system CSS compilable
- Ready for visual verification in Phase 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-quick-wins/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Quick Wins CSS Summary

**[One-liner summary of what was done]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.css` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-02-PLAN.md (Visual verification)
</output>
